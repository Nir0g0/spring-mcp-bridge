spring:
  datasource:
    url: jdbc:h2:mem:mcpbridge-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
      mode: never
  mcp:
    transport: sse
    security:
      enabled: false

server:
  port: 0
